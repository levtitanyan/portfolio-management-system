I did not change any files.

Findings

High: your quoted result is not an apples-to-apples win over buy-and-hold. It is the TCN 30-day result in metrics_30d.json (line 35). The strategy reports 18 * 30 = 540 trading days, while buy-and-hold reports only 532 daily periods in the same JSON at line 59 (line 59). The 30-day strategy’s last trade includes forward returns after the benchmark’s last date.

High: using raw prices, a fairer buy-and-hold window for that TCN 30-day run is roughly 2024-01-31 to 2026-03-31, because predictions start 2024-01-31 and the final 30-day target extends past 2026-02-17. That buy-and-hold result is about $15,547 / +55.5%, which is higher than your quoted TCN $14,984 / +49.8%.

High: the quoted model does not show strong predictive skill. In the same file, test directional accuracy is 46.7% and information coefficient is -0.0187 at metrics_30d.json (line 29). Long-short also loses money, which usually means the ranking signal is weak or inverted.

Medium: targets are built before splitting, so boundary rows leak future-period labels across train/validation/test boundaries. The forward targets are created at 2_build_features.py (line 289), then splits happen later at 3_prepare_model_data.py (line 136). For 30-day targets, late train labels include validation returns, and late validation labels include test returns.

Medium: the “energy30” run actually used 29 stocks, not 30. HES is in the universe at universes.py (line 37), but it is absent from data/energy30/raw; your prediction files also show 29 tickers. The downloader logs failures but still exits successfully at 1_download_yahoo_data.py (line 126), so the pipeline can silently continue with fewer stocks.

Medium: random forest “time weighting” is not really time weighting. Splits are sorted by ticker, Date at 4_train_baselines.py (line 113), then np.linspace weights rows at line 249 (line 249). That gives later alphabetic tickers more weight, not simply newer dates.

Medium: the training-script backtest and portfolio_backtest.py use different assumptions. The helper backtest charges a flat cost at backtest_utils.py (line 218), while portfolio_backtest.py uses turnover and drawdown controls around portfolio_backtest.py (line 229). That is why your quoted TCN 30-day top-5 result differs from the final portfolio report.

Bottom Line

I would not claim yet that this model reliably beats buy-and-hold. The quoted +49.8% result is weakened by benchmark horizon mismatch, weak/negative ranking metrics, and long-short underperformance.

The encouraging part: the code is not obviously “cheating everywhere,” and some final portfolio_backtest.py rows still look interesting. But before calling it real alpha, fix the benchmark horizon, enforce exactly 30 tradable stocks or fail loudly, remove split-boundary leakage, and evaluate a pre-chosen strategy/top-k rather than selecting the best result after seeing test performance.